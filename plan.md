---

# 《暴力摩托》完整开发计划

## 技术栈总览

开发语言是 TypeScript + Phaser 3，构建工具是 Vite（比 Webpack 快很多，配置也更简洁），包管理器是 npm。伪 3D 公路层使用原生 Canvas API 直接绘制，Phaser 负责 Scene 管理、键盘/触摸输入、Audio Manager、UI 精灵层。整个项目会组织成 `src/road/`（线扫描引擎）、`src/entities/`（玩家/AI/警察）、`src/scenes/`（Phaser 场景）、`src/systems/`（战斗/物理/关卡）几个模块。

---

## V1：能跑的公路——伪 3D 渲染引擎

**本版本目标：** 启动游戏，看到一条向远处延伸的公路，能骑车跑起来，有速度感，有弯道，摩托车不会穿出路边。这是整个游戏的地基，后面所有东西都建立在这一层上。

**版本核心概念：线扫描投影。** 屏幕上每一条水平扫描线（scanline）对应公路在某个深度 `z` 处的横截面。深度越大（越远），该扫描线对应的公路宽度在屏幕上看起来越窄，位置越靠近地平线。通过对每条线应用不同的缩放因子和水平偏移，就能"骗"出透视感。

**TODO 1.1 — 项目脚手架与开发环境（半天）**

用 Vite + TypeScript 模板初始化项目，安装 `phaser`，配置 `tsconfig.json` 开启严格模式和路径别名（`@/` 指向 `src/`）。创建 `index.html`，在里面放一个 `<div id="game">`，再放一个**叠加在上面的**原生 `<canvas id="road-canvas">` 用于线扫描渲染，Phaser 的 canvas 用于 UI 层。理解这个双层 canvas 架构是关键：路面永远在最底层，Phaser 的 UI 和精灵浮在上面。

**TODO 1.2 — Z 映射表（Z-Map）的数学推导与实现（一天）**

这是线扫描引擎的核心数学。预计算一个数组 `zMap[screenY]`，把屏幕上每一条水平线的 Y 坐标映射到世界空间中的深度值 `z`。公式推导如下：设摄像机高度为 `camHeight`，地平线在屏幕 Y 坐标 `horizon` 处，则 `zMap[y] = camHeight * screenHeight / (y - horizon)`。用这个 `z` 值可以算出该扫描线的缩放因子 `scale = 1 / z`，再用 `scale` 乘以道路半宽 `roadWidth`，得到该扫描线上道路在屏幕上的像素宽度。写一个独立的 `ZMapper` 类，暴露 `getZ(screenY)` 和 `getScale(screenY)` 方法。花半天时间用控制台把这些数值打印出来，画个草图理解它们的变化规律，不要急着渲染。

**TODO 1.3 — 静态公路绘制（一天）**

实现 `RoadRenderer` 类，逐行扫描屏幕从地平线到底部，对每条扫描线计算道路的左右边缘 X 坐标，然后用 `ctx.fillRect` 画一个宽度等于道路宽度、高度为 1px 的矩形。用颜色区分：天空是深灰 `#1a1a2e`，草地是深绿 `#1e3a1e`，路面交替用 `#404040` 和 `#505050` 产生斑马条纹感（通过 `Math.floor(z / stripeLength) % 2` 判断奇偶行切换颜色），道路边缘白线是 `#ffffff`。不需要任何动态效果，先把静态透视图画出来对。

**TODO 1.4 — 摄像机前进与速度感（一天）**

引入一个全局变量 `cameraZ`，每帧按玩家速度递增。把 Z 映射改为 `zMap[y] = camHeight * screenHeight / (y - horizon) + cameraZ`。关键：斑马纹的奇偶判断要用 `Math.floor((z + cameraZ) / stripeLength) % 2`，这样条纹就会向玩家扑面而来而不是静止不动。在 Phaser 的 `update()` 里驱动 `cameraZ` 增长，感受速度感是否正确。

**TODO 1.5 — 键盘输入与摩托车横向移动（半天）**

用 Phaser 的 `this.input.keyboard.createCursorKeys()` 捕获左右方向键，控制玩家的世界坐标 `playerX`（范围 -1 到 +1，0 是道路中心）。在渲染层，根据 `playerX` 偏移每条扫描线的绘制起始 X，产生"我在向左/右走"的视觉感。摩托车本体用一个简单的彩色矩形块代替：`ctx.fillStyle = '#ff4444'`，在屏幕底部中央画一个约 40×60 像素的矩形。

**TODO 1.6 — 碰撞检测：不能驶出路面（半天）**

在每帧更新时，用 `cameraZ` 对应的扫描线的道路左右边缘 X 值，与玩家 `playerX` 做比较。如果 `playerX` 超出边缘，强制速度减半、`playerX` 钳位到边缘值，模拟"压路肩减速"效果。

**V1 完成里程碑：** 能看到一条无限延伸的公路向你扑来，摩托车能左右移动，离开路面会减速，画面有基本的透视感。

---

## V2：弯道与坡度——公路几何系统

**本版本目标：** 公路不再是直线，加入弯道和上下坡，给公路赋予地形数据，摄像机跟随弯道偏转，视觉上形成真正的"转弯"感。这是技术难度最高的版本，需要一些耐心。

**TODO 2.1 — 公路段落数据结构（半天）**

设计 `RoadSegment` 接口：包含 `curve`（弯道曲率，正值右弯负值左弯）和 `hill`（坡度，影响地平线高度变化）两个字段。创建 `TrackData` 数组，手动写几段测试数据：直道 500 米、右弯 200 米、直道 300 米、左弯……这个数组就是你的关卡地形描述文件。

**TODO 2.2 — 累积曲率投影（一天半）**

这是弯道渲染最核心的技术。在每帧绘制时，从玩家当前 `cameraZ` 出发，向远处遍历即将要绘制的每条扫描线对应的 `RoadSegment`，累积它们的 `curve` 值，得到一个 `cumulativeCurve`。用这个累积曲率值水平偏移每条扫描线的绘制起始 X：越远处的扫描线偏移越大，就产生了弯道向一侧弯曲的视觉效果。公式：`drawX = baseX + cumulativeCurve * scale`。花时间调参，让弯道看起来"顺滑"而不是"突变"。

**TODO 2.3 — 坡度与地平线变化（一天）**

用 `hill` 值累积影响地平线的 Y 坐标。上坡时地平线 Y 升高（视野抬起），下坡时地平线 Y 降低（视野压下）。重新计算每帧的 `horizon` 值，让 ZMapper 用动态的 `horizon` 重新生成 zMap。注意：坡度变化会让地平线以上的天空区域大小变化，需要同步重绘天空矩形。

**TODO 2.4 — 转弯手感：玩家 X 受曲率影响（半天）**

现实中转弯时，如果你不主动转向，惯性会让你偏向弯道外侧。模拟这一点：每帧给 `playerX` 加上一个小量 `currentSegment.curve * speed * curvePull`，迫使玩家主动操舵才能保持在弯道内。这大幅提升了驾驶手感的真实感。

**TODO 2.5 — 关卡编辑工具（可选，半天）**

写一个简单的 HTML 页面，用 `<textarea>` 让你输入一段 JSON 描述的 `TrackData`，实时预览渲染结果。不必做得精美，目的是让后续关卡设计变得可迭代而不是每次改代码。

**V2 完成里程碑：** 公路有弯道和上下坡，转弯时视觉效果正确，驾驶手感明显好于直线公路。

---

## V3：实体系统——对手、交通和碰撞

**本版本目标：** 公路上不再是空的，加入 AI 对手摩托车和普通交通车辆，用彩色矩形代替精灵，有基本的碰撞逻辑。

**TODO 3.1 — 实体世界坐标系设计（半天）**

每个实体（对手、交通车）存储 `{ worldZ: number, laneX: number, speed: number, width: number, height: number, color: string }`。`worldZ` 是其在公路上的绝对纵向距离，`laneX` 是 -1 到 +1 的横向位置。写一个 `EntityManager` 类，负责管理所有实体的 update 和 render。

**TODO 3.2 — 实体的伪 3D 投影渲染（一天）**

这是实体渲染的核心挑战。你需要把一个实体的 `worldZ` 转换成屏幕 Y 坐标（反查 zMap），再用对应深度的 `scale` 值缩放其宽高，最后考虑累积曲率偏移其 X 位置。对于玩家后方的实体（`worldZ < cameraZ`）直接跳过不渲染，对于超过绘制距离的实体也跳过。用 `ctx.fillRect` 画一个对应颜色的矩形，上半部分是车手颜色，下半部分是摩托车颜色，用高度差来模拟"人骑在车上"的形态。

**TODO 3.3 — 深度排序（Painter's Algorithm）（半天）**

多个实体在 canvas 上必须按从远到近的顺序绘制，否则近处实体会被远处实体遮挡。每帧将所有可见实体按 `worldZ` 降序排列（最远的先画），这就是经典的画家算法。Phaser 的 Sprite 深度系统无法用于这里，必须在手动 canvas 渲染循环里自己控制顺序。

**TODO 3.4 — AI 对手基础行为（一天）**

实现 `AIRider` 类，有三个行为状态：正常行驶（维持目标速度，缓慢向车道中心靠拢）、躲避（检测到前方有障碍时随机换道）、被攻击（受到打击后速度短暂下降）。使用简单的状态机（State Machine）管理状态转换：每个状态有 `enter()`、`update(dt)` 方法。AI 速度略低于玩家最大速度，确保玩家能追上去打。

**TODO 3.5 — 玩家与实体的碰撞检测（一天）**

碰撞检测在世界坐标系里做，不在屏幕坐标里做。条件是：`|playerZ - entityZ| < hitDepth && |playerX - entityX| < hitWidth`。碰到普通交通车：玩家速度大幅降低，触发"摔车"动画（画面抖动 + 摩托车矩形消失 0.5 秒）。碰到 AI 对手时触发"推挤"效果（双方都轻微偏移 X）。

**V3 完成里程碑：** 公路上有 AI 对手和交通车，会被追上和碰到，有基本的碰撞反馈。

---

## V4：战斗系统——拳击、踢腿与武器

**本版本目标：** 这是暴力摩托区别于普通赛车游戏的核心。实现近战攻击、武器拾取和武器使用，AI 也会反击。

**TODO 4.1 — 攻击判定范围设计（半天）**

攻击必须在"近战范围"内才能命中。定义：`playerZ` 与目标的 `worldZ` 差值小于 `meleeRange`（约 1.5 个车身长度），且 `|playerX - targetX|` 小于 `meleeWidth`（约 1 个车身宽度）。引入 `nearbyTarget` 概念，每帧找出距离玩家最近且在近战范围内的实体，这就是当前的攻击目标。

**TODO 4.2 — 拳击与踢腿动作（一天）**

按 `Z` 键出左拳，`X` 键出右拳，`空格` 踢腿。引入 `attackCooldown` 防止连续乱按。命中时，目标的 `health` 减少，`speed` 短暂降低（被打会减速），同时在屏幕上叠加一个用彩色矩形模拟的"打击闪光"（命中位置出现一个白色矩形，持续 3 帧）。将攻击结果用 Phaser 的文字对象显示在目标上方：`HIT!`、`MISS`（依据一个简单的命中概率公式）。

**TODO 4.3 — 击倒与复原机制（一天）**

当目标 `health <= 0`，触发"击倒"：目标在屏幕上做一个夸张的飞出动画（`entityX += knockbackForce`，连续几帧偏移增大），然后在 1.5 秒后于玩家后方某处重新"爬起来"（重置 health，重置 worldZ 到玩家后方一段距离）。玩家自己被击倒时，同样触发复原流程，期间无敌帧 2 秒。这个"被打飞但还会回来"的设计是原版暴力摩托的精髓之一。

**TODO 4.4 — 武器系统（一天）**

设计武器类型：`FIST`（无武器）、`CLUB`（球棒，伤害+50%）、`CHAIN`（铁链，射程+30%）。武器来源有两种：从 AI 对手手中抢夺（攻击成功时有 30% 概率缴械并获得对方武器），或者在公路边缘随机出现的道具盒（用闪烁的彩色矩形表示，骑过去自动捡起）。当前武器显示在 HUD 左下角，用文字标注名称和剩余耐久度（每次命中消耗 1 点，耐久归零变回空手）。

**TODO 4.5 — AI 反击行为（一天）**

扩展 `AIRider` 状态机，加入 `ATTACKING` 状态：当 AI 进入玩家近战范围时，有一定概率发起反击。反击逻辑：AI 发出攻击指令，0.3 秒后判定命中/未命中（给玩家留出躲避窗口）。命中玩家时显示红色闪光，玩家速度减少。不同 AI 难度对应不同的反击频率和命中概率（简单 AI 反击慢、命中率低，困难 AI 反应快、出招多）。

**V4 完成里程碑：** 能骑车追上对手然后把他打下去，有多种武器，AI 会反打，整个战斗循环跑通。

---

## V5：警察系统

**本版本目标：** 暴力摩托的第二大张力来源——追你的警察。

**TODO 5.1 — 警察触发逻辑（半天）**

引入全局 `heatLevel`（0 到 5），每次打人 +1、打倒对手 +2、超速 10 秒 +1。`heatLevel` 达到 2 时，在玩家后方一段距离生成第一辆警察摩托（用蓝白色矩形标识）。`heatLevel` 越高，警察数量越多（最多同时 3 辆），速度也越快。`heatLevel` 随时间自然衰减（每 5 秒 -0.5），不打人、不超速就能慢慢甩掉风险。

**TODO 5.2 — 警察 AI 追逐行为（一天）**

警察的 AI 逻辑：始终向玩家当前 `laneX` 靠近，速度目标是玩家速度 +10%（确保能追上），在进入近战范围时不打人而是"逼迫"（对玩家施加横向推力，把你推向路肩），最终目标是迫使玩家撞上路肩触发摔车——摔车状态下玩家无法操作 1.5 秒，警察在这段时间内到达就触发"被捕"事件。警察也可以被玩家攻击（被打下去后 `heatLevel` +2 作为惩罚，但警察会在更短的时间内复原归队）。

**TODO 5.3 — 被捕与逃脱的后果系统（半天）**

被捕后进入"被捕"场景：用 Phaser 场景显示一个黑底文字界面，扣除本关卡奖励金的 30% 作为罚款，若金钱不足则直接游戏失败。逃脱警察（甩掉所有追警超过 10 秒）`heatLevel` 清零，给一个"成功逃脱"的文字奖励。

**V5 完成里程碑：** 警察会来追你，被抓有惩罚，系统压力感建立起来了。

---

## V6：完整游戏循环——多关卡、奖金、升级

**本版本目标：** 把前几个版本的系统整合成一个有开始有结束有进展感的完整游戏。

**TODO 6.1 — 关卡数据系统（一天）**

设计 `LevelConfig` 接口，包含：关卡名称、`TrackData`（公路几何）、参赛对手列表（AI 类型和数量）、时间限制、完成奖金、交通车密度、警察出现阈值。手工创建 5 个关卡，难度递进：第一关直道多、对手少、警察慢；第五关弯道密集、对手凶、警察速度逼近玩家上限。

**TODO 6.2 — 比赛排名与完赛判定（半天）**

在 HUD 右上角实时显示当前名次（根据所有实体的 `worldZ` 排名）。到达终点（`cameraZ > trackLength`）触发完赛：计算玩家名次，Top 4 名次才能过关（原版设定），否则显示"淘汰"界面。完赛后发放奖金，进入赛后结算 Phaser 场景。

**TODO 6.3 — 车手档案与摩托车升级（一天半）**

设计持久化存储结构（用 `localStorage` 存档）：`{ money, currentBike, unlockedLevels, bestTimes }`。设计 3 种摩托车（用不同颜色和尺寸的矩形区分，附带不同的数值：`topSpeed`、`acceleration`、`handling`）。在关卡选择场景里加入"车库"界面，花钱升级摩托车属性或购买新车。这是原版暴力摩托的核心动力循环：打赢比赛 → 赚钱 → 买更快的车 → 打更难的比赛。

**TODO 6.4 — Phaser 场景流程串联（一天）**

实现完整的场景流程：`BootScene`（加载资源）→ `MainMenuScene`（主菜单）→ `GarageScene`（车库/升级）→ `LevelSelectScene`（关卡选择）→ `RaceScene`（核心游戏）→ `ResultScene`（结算）→ 回到 `GarageScene` 或 `GameOverScene`。用 Phaser 的 `this.scene.start('SceneName', data)` 传递数据，避免全局状态污染。

**TODO 6.5 — HUD 完整化（半天）**

在 Phaser 的 UI 层（Camera 固定的 container）上渲染：速度计（数字文字，可以后期换成弧形图形）、名次、剩余时间倒计时、`heatLevel` 警示条（5 格，逐渐变红）、当前武器图标、摩托车血量条（摔车会扣血，血量归零摩托车损毁需要花钱修）。

**V6 完成里程碑：** 可以从主菜单开始，打几关，赚钱，买车，有完整的进存档，游戏有头有尾。

---

## V7：打磨与扩展

**本版本目标：** 在完整游戏基础上加入音效、视觉反馈优化、难度调平和一些加分项。

**TODO 7.1 — Web Audio API 程序化音效（一天）**

不需要任何音效资源文件，用 `AudioContext` + `OscillatorNode` 实时合成：引擎声（低频正弦波，频率随速度变化）、碰撞音（白噪声短脉冲）、警笛声（双频调制正弦波交替）。Phaser 的 `Phaser.Sound.WebAudioSoundManager` 可以直接接管 `AudioContext`。

**TODO 7.2 — 背景音乐程序化生成（一天）**

用 Web Audio API 的 `SequencerNode` 写一个简单的 4/4 拍节奏引擎，播放硬岩风格的重复节拍（鼓机 + 失真吉他模拟）。不需要真正的乐器采样，全部用合成波形。这是整个计划中唯一比较"额外"的项，如果时间紧可以直接用 `Howler.js` 播放一段 CC0 授权的背景音乐代替。

**TODO 7.3 — 视觉特效强化（一天）**

加入屏幕抖动（canvas `translate` 随机偏移 2-5px，持续 5 帧）用于碰撞反馈；加入运动模糊（在绘制每帧之前先用半透明黑色覆盖整个 canvas：`ctx.fillStyle = 'rgba(0,0,0,0.1)'`，让上一帧图像略微残留，高速时更明显）；加入地平线雾气（距离远的扫描线颜色向天空色插值，`lerp(roadColor, skyColor, fogFactor)`）。

**TODO 7.4 — 难度平衡（一天）**

写一个 `BalanceTest` 脚本，模拟 AI 对手跑完所有关卡，记录通关率数据，据此调整各关的对手速度上限、警察出现时机和攻击频率。这是进阶水平开发者最容易忽视但玩家体验差距最大的环节。

**TODO 7.5 — 移动端触摸控制（半天）**

在屏幕左侧绘制虚拟左右方向键区域，右侧绘制攻击按钮。用 Phaser 的 `this.input.on('pointerdown')` 处理触摸，保证手机上可玩。

**TODO 7.6 — 部署（半天）**

`vite build` 生成静态文件，推送到 GitHub，用 GitHub Pages 一键部署。游戏 URL 即可分享给任何人在浏览器里直接玩，无需安装。

---

## 总结与关键路径

下面是整个计划的核心逻辑链，理解它能帮你在遇到问题时知道自己卡在哪个层：

**V1（公路数学）→ V2（地形数学）→ V3（实体系统）→ V4（战斗系统）→ V5（警察）→ V6（游戏循环）→ V7（打磨）**

其中 V1 + V2 是整个项目最难啃的部分，因为线扫描数学没有 Phaser 可以依赖，完全是你自己的代码。一旦这两个版本过了，后面的 V3-V6 本质上都是"在一个稳定的渲染引擎上构建游戏逻辑"，难度会大幅下降，而且每个版本都能快速看到效果带来的正反馈。

---

一个实用建议：每个版本完成后，花半天时间**重构一遍**刚写的代码，把魔法数字提取成常量，把重复逻辑封装成函数。赛车游戏的很多 bug（比如对手在弯道里飞出去、警察卡在路边）都来自于早期代码里混乱的坐标系和未处理的边界条件，早期养成习惯能省去后期大量的调试时间。
